meta {
  name: staff_movies_new
  type: http
  seq: 2
}

post {
  url: {{SERVER_URL}}/api/staff/:id/movies
  body: json
  auth: inherit
}

params:path {
  id: 30
}

body:json {
  {"roleName": "Abuelita", "movieId": 1}
}

settings {
  encodeUrl: true
  timeout: 0
}
