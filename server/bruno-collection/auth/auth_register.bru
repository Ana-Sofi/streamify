meta {
  name: auth_register
  type: http
  seq: 1
}

post {
  url: {{SERVER_URL}}/api/auth/register
  body: json
  auth: none
}

body:json {
  {
    "name": "Jon",
    "lastName": "Admin",
    "email": "admin.user@streamify.com",
    "password": "mysecretpassword"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
